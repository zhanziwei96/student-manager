# ClassHub 测试依赖
# 安装: pip install -r tests/requirements-test.txt

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-html>=3.2.0

# HTTP 测试
requests>=2.31.0
httpx>=0.25.0

# 性能测试（可选）
# locust>=2.18.0
# k6 需要通过其他方式安装

# 代码覆盖率（可选）
pytest-cov>=4.1.0

# 测试报告（可选）
allure-pytest>=2.13.0
